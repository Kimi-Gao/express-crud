{
  "name": "my-projects",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "tsx index.ts",
    "test": "echo \"no test specified\" && exit 0",
    "ci": "npm run test"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "tsx": "^4.19.4"
  }
}
